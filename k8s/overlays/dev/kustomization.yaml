apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: compaas-dev

resources:
  - namespace.yaml
  - ../../base
  - service.yaml

configMapGenerator:
  - name: compaas-cm
    behavior: merge
    env: config.properties

patchesStrategicMerge:
  - deployments.yaml
