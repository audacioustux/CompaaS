{
    "id": "graalvm",
    "name": "GraalVM JDK",
    "installsAfter": [
        "./extra-utils"
    ],
    "containerEnv": {
        "JAVA_HOME": "/opt/graalvm",
        "GRAALVM_HOME": "${JAVA_HOME}",
        "PATH": "${PATH}:${GRAALVM_HOME}/bin"
    }
}