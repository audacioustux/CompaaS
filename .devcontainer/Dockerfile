FROM mcr.microsoft.com/devcontainers/base:ubuntu-22.04

ARG GRAAL_EE_DOWNLOAD_TOKEN
ENV GRAAL_EE_DOWNLOAD_TOKEN ${GRAAL_EE_DOWNLOAD_TOKEN}
ARG GRAALVM_HOME /opt/graalvm
ENV GRAALVM_HOME ${GRAALVM_HOME}
